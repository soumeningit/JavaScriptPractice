# JavaScript Object-Related Questions

Here’s a comprehensive list of **JavaScript object-related questions**, ranging from basic to advanced. This can be useful for interviews, study, or mastery of JavaScript objects.

---

## 🔹 **Basic Questions**

1. What is an object in JavaScript?
2. How do you create an object in JavaScript?
3. What are the different ways to define an object?
4. How do you access object properties?
5. What is the difference between dot notation and bracket notation?
6. How do you add or update a property of an object?
7. How do you delete a property from an object?
8. How can you check if a property exists in an object?
9. What is the `in` operator?
10. What does `hasOwnProperty()` do?

---

## 🔹 **Intermediate Questions**

11. What is object destructuring?
12. How does object cloning work in JavaScript?
13. What is the difference between shallow and deep copy?
14. How do you clone an object using `Object.assign()`?
15. How do you clone an object using the spread operator?
16. What are enumerable properties?
17. What is the difference between `Object.keys()`, `Object.values()`, and `Object.entries()`?
18. How do you loop through object properties?
19. What is the difference between `for...in` and `for...of`?
20. How does `Object.freeze()` work?
21. How does `Object.seal()` differ from `Object.freeze()`?
22. What is the prototype chain?
23. What is the difference between `__proto__` and `prototype`?
24. What is the `this` keyword in the context of an object?
25. What is method shorthand syntax in object literals?
26. How do getters and setters work in objects?
27. What is object literal enhancement?

---

## 🔹 **Advanced Questions**

28. What is `Object.create()` and how does it work?
29. What are symbols and how are they used as object keys?
30. What is a computed property name?
31. How do you merge objects?
32. What is `Object.defineProperty()` and `Object.defineProperties()`?
33. What are property descriptors?
34. How do you make an object immutable?
35. How does JavaScript handle object references in functions?
36. How does garbage collection work with objects?
37. How do JavaScript objects differ from maps?
38. What is the `toString()` method in objects?
39. How do you override `valueOf()` and `toString()` in an object?
40. What is a plain object vs a class instance?

---

## 🔹 **ES6+ and Modern JavaScript Features**

41. How do classes relate to objects?
42. What is `super` in an object method?
43. What is object spread and rest (`...`)?
44. How do private properties work in classes/objects (using `#`)?
45. What are object proxies and how are they used?
46. What is the purpose of `Object.fromEntries()`?
47. What is the use of `structuredClone()`?

---

## 🔹 **Tricky / Gotcha Questions**

48. What happens when you stringify a circular object?
49. Can object keys be numbers?
50. Can object keys be objects?
51. What is the difference between an object and an array?
52. What’s the difference between JSON and a JavaScript object?
53. How do you compare two objects for equality?
54. What does `Object.is()` do?
55. What happens when you pass an object as a function parameter?

---

_Would you like these categorized into flashcards, a quiz, or interview prep format?_
